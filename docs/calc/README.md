# Расчёты к проверке теорий

Скрипты, которыми получены числа в [`../architecture-review.md`](../architecture-review.md).
Зависимостей нет, только стандартная библиотека.

```
python3 takty-i-rashod.py   # §3 такты, §8 расход, пики, цена модели, рост decision → пункты C и J
python3 petlya-very.py      # §6 коэффициент усиления и окно настройки → пункт B
```

Все константы взяты из `architecture.md` и помечены ссылкой на раздел. Если такт,
число поселений или период решения меняются — правится здесь, и выводы пересчитываются.
